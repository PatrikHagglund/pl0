# Stray in-tree build outputs from manual (non-bazel) compiles. NOTE: must
# stay anchored or dir-scoped now that e0/..e6/ are real source directories
# (a bare "e1" pattern would ignore the whole e1/ tree).
/e1_compile
/e1_llvm
out
out.ll
out.cpp
out_cpp
out-O0
.image
*~
\#*#
.koka/
e1/e1
e1/e1peg
e2/e2peg
e3/e3peg
e1/e1_rt_bigint.ll

# Bazel
bazel-*
.bazel-cache

# Stray empty file repeatedly created by something in the dev environment
oom

# Claude Code recreates an empty CLAUDE.md; this repo uses AGENTS.md instead
CLAUDE.md
